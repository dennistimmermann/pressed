{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM"],
    "types": ["vite/client"],
    // The library .vue files import from 'pressed' — the module the runtime frame injects.
    "paths": { "pressed": ["./src/template/pressed-module.ts"] }
  },
  "include": ["src", "runtime"]
}
